diff --git a/com/android/build/gradle/internal/ndk/NdkR25Info.kt b/com/android/build/gradle/internal/ndk/NdkR25Info.kt
--- a/com/android/build/gradle/internal/ndk/NdkR25Info.kt
+++ b/com/android/build/gradle/internal/ndk/NdkR25Info.kt
@@ -92,7 +92,13 @@ class NdkR25Info(private val rootDirectory: File) : NdkInfo {
+    // Modified by Zyntax: select the native Linux toolchain for the JVM host architecture.
     private val hostTag: String by lazy {
-        when(currentPlatform()) {
+        when (currentPlatform()) {
             PLATFORM_WINDOWS -> "windows-x86_64"
-            PLATFORM_DARWIN-> "darwin-x86_64"
-            else -> "linux-x86_64"
+            PLATFORM_DARWIN -> "darwin-x86_64"
+            PLATFORM_LINUX -> when (val architecture = System.getProperty("os.arch")) {
+                "aarch64", "arm64" -> "linux-arm64"
+                "amd64", "x86_64" -> "linux-x86_64"
+                else -> error("Unsupported NDK host architecture: $architecture")
+            }
+            else -> error("Unsupported NDK host operating system: ${System.getProperty("os.name")}")
         }
     }
diff --git a/com/android/build/gradle/internal/cxx/configure/NdkLocator.kt b/com/android/build/gradle/internal/cxx/configure/NdkLocator.kt
--- a/com/android/build/gradle/internal/cxx/configure/NdkLocator.kt
+++ b/com/android/build/gradle/internal/cxx/configure/NdkLocator.kt
@@ -39,7 +39,6 @@ import com.android.utils.cxx.CxxDiagnosticCode.NDK_VERSION_IS_UNMATCHED
 import com.android.utils.cxx.CxxDiagnosticCode.NDK_VERSION_IS_UNMATCHED
 import com.android.utils.cxx.CxxDiagnosticCode.NDK_VERSION_UNSUPPORTED
 import com.google.common.annotations.VisibleForTesting
-import org.gradle.api.InvalidUserDataException
 import org.gradle.api.provider.ProviderFactory
 import java.io.File
 
@@ -72,7 +71,8 @@ fun findNdkPathImpl(
     sdkFolder: File?,
     ndkVersionedFolderNames: List<String>,
     getNdkSourceProperties: (File) -> SdkSourceProperties?,
-    sdkHandler: SdkHandler?
+    sdkHandler: SdkHandler?,
+    issueReporter: IssueReporter
 ): NdkLocatorRecord? {
     // Record status of user-supplied information
     logUserInputs(
@@ -157,7 +157,8 @@ fun findNdkPathImpl(
                         sdkFolder,
                         ndkVersionedFolderNames,
                         getNdkSourceProperties,
-                        sdkHandler = null // Don't want to download hypothetical NDK
+                        sdkHandler = null, // Don't want to download hypothetical NDK
+                        issueReporter = issueReporter
                     )
                     if (resolutionWithoutNdkDir == resolutionWithNdkDir) {
                         infoln("Deleting ndk.dir and setting android.ndkVersion to " +
@@ -246,13 +247,14 @@ fun findNdkPathImpl(
         return NdkLocatorRecord(downloaded, revisionFromNdkVersion)
     }
 
-    // Throw error expected by Android Studio. If the text isn't this, then Android Studio won't
-    // recognize the error and provide hyperlink to install NDK.
-    // TODO(b/131320700) convert to errorln(..) which requires co-updating Android Studio.
-    throw InvalidUserDataException(
-        "NDK not configured. Download " +
-                "it with SDK manager. Preferred NDK version is " +
-                "'$NDK_DEFAULT_VERSION'. ")
+    // Modified by Zyntax: normal builds fail through the existing issue reporter;
+    // IDE sync retains a blocking package issue without fabricating native configuration.
+    issueReporter.reportError(
+        IssueReporter.Type.MISSING_SDK_PACKAGE,
+        "NDK $revisionFromNdkVersion could not be located or installed.",
+        "$FD_NDK_SIDE_BY_SIDE;$revisionFromNdkVersion"
+    )
+    return null
 }
 
 /**
@@ -429,7 +431,8 @@ data class NdkLocator(
             sdkPath,
             getNdkVersionedFolders(File(sdkPath, FD_NDK_SIDE_BY_SIDE)),
             ::getNdkVersionInfo,
-            if (downloadOkay) sdkHandler else null
+            if (downloadOkay) sdkHandler else null,
+            issueReporter
         )
     }
 }
diff --git a/com/android/build/gradle/internal/ide/v2/AndroidDslImpl.kt b/com/android/build/gradle/internal/ide/v2/AndroidDslImpl.kt
--- a/com/android/build/gradle/internal/ide/v2/AndroidDslImpl.kt
+++ b/com/android/build/gradle/internal/ide/v2/AndroidDslImpl.kt
@@ -19,2 +19,3 @@
 import com.android.builder.model.v2.dsl.BuildType
+import com.android.builder.model.v2.dsl.CmakeOptions
 import com.android.builder.model.v2.dsl.DependenciesInfo
@@ -39,3 +39,4 @@
     override val buildToolsVersion: String,
     override val dependenciesInfo: DependenciesInfo?,
+    override val cmake: CmakeOptions?,
 ): AndroidDsl, Serializable {
diff --git a/com/android/build/gradle/internal/ide/v2/ModelBuilder.kt b/com/android/build/gradle/internal/ide/v2/ModelBuilder.kt
--- a/com/android/build/gradle/internal/ide/v2/ModelBuilder.kt
+++ b/com/android/build/gradle/internal/ide/v2/ModelBuilder.kt
@@ -42,3 +42,4 @@
 import com.android.build.gradle.internal.BuildTypeData
 import com.android.build.gradle.internal.ProductFlavorData
+import com.android.build.gradle.internal.SdkComponentsBuildService
 import com.android.build.gradle.internal.VariantDimensionData
@@ -54,3 +54,4 @@
 import com.android.build.gradle.internal.component.TestVariantCreationConfig
 import com.android.build.gradle.internal.component.VariantCreationConfig
+import com.android.build.gradle.internal.cxx.configure.defaultCmakeVersion
 import com.android.build.gradle.internal.dependency.AdditionalArtifactType
@@ -768,5 +768,17 @@
         val compileSdkVersion = extensionImpl.compileSdkVersion ?: "unknown"
 
+        // Modified by Zyntax: transport configuration without locating or executing native tools.
+        val cmake = extension.externalNativeBuild.cmake
+        val cmakeOptions = if (cmake.path == null) null else {
+            val sdkComponents = getBuildService(project.gradle.sharedServices, SdkComponentsBuildService::class.java).get()
+            CmakeOptionsImpl(
+                requestedVersion = cmake.version,
+                defaultVersion = defaultCmakeVersion.toString(),
+                customDirectory = sdkComponents.cmakeDirFromProperties?.let { File(it) },
+            )
+        }
+
         return AndroidDslImpl(
             buildToolsVersion = extension.buildToolsVersion,
+            cmake = cmakeOptions,
             groupId = project.group.toString(),
diff --git a/com/android/build/gradle/internal/ide/v2/CmakeOptionsImpl.kt b/com/android/build/gradle/internal/ide/v2/CmakeOptionsImpl.kt
new file mode 100644
--- /dev/null
+++ b/com/android/build/gradle/internal/ide/v2/CmakeOptionsImpl.kt
@@ -0,0 +1,27 @@
+/*
+ * Copyright (C) 2026 Zyntax
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+package com.android.build.gradle.internal.ide.v2
+
+import com.android.builder.model.v2.dsl.CmakeOptions
+import java.io.File
+import java.io.Serializable
+
+data class CmakeOptionsImpl(
+    override val requestedVersion: String?,
+    override val defaultVersion: String,
+    override val customDirectory: File?,
+) : CmakeOptions, Serializable
